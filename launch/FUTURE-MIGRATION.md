# Optional future EZRaiderEU → VOY PRO migration

This file is **not** part of the current launch plan.

The old EZRaiderEU Budapest site remains live in phase 1. If a later business decision is made to consolidate it into VOY PRO, use `future-migration-map.csv` as a starting point, re-crawl the old site, rebuild the mapping from the then-current index, install direct permanent redirects and only then consider Search Console Change of Address.

Do not install these redirects during the parallel-site phase.
